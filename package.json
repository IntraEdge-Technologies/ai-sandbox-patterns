{
  "name": "ai-sandbox-patterns",
  "version": "0.1.0",
  "description": "Three sandboxing patterns for the Claude Agent SDK runtime. Companion repo for the talk.",
  "private": true,
  "type": "module",
  "scripts": {
    "a": "npx tsx agent/agent-a.ts",
    "b": "npx tsx agent/agent-b.ts",
    "c": "sbx exec -e PATTERN=C -e ANTHROPIC_API_KEY=$ANTHROPIC_API_KEY -w $(pwd) pattern-c npx tsx agent/agent-c.ts",
    "dashboard": "tsx dashboard/server.ts"
  },
  "dependencies": {
    "@anthropic-ai/claude-agent-sdk": "^0.2.108",
    "e2b": "^2.19.0",
    "fastify": "^5.8.5",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "tsx": "^4.21.0",
    "typescript": "^6.0.2"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT"
}
