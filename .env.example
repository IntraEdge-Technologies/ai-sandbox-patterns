# Required: get one at https://console.anthropic.com
ANTHROPIC_API_KEY=sk-ant-...

# Required for Pattern B (host agent + remote E2B microVM tool sandbox).
# Get one at https://e2b.dev/dashboard. Free tier available.
E2B_API_KEY=e2b_...

# This secret is set by the dashboard server automatically.
# It exists here to document the demo's "canary secret" design:
# Patterns A/B can read it (agent on host). Patterns C/D cannot (agent in sandbox).
# SECRET_DATABASE_PASSWORD=prod-db-hunter2
